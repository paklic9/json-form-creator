export enum ContentType {
  Config = "Config",
  Result = "Result",
}

export enum ButtonType {
  SaveWithReset = "SaveWithReset",
  Reset = "Reset",
  Save = "Save",
  Unsupported = "Unsupported",
}

export enum ItemType {
  Number = "Number",
  String = "String",
  Text = "Text",
  Boolean = "Boolean",
  Date = "Date",
  List = "List",
  Unsupported = "Unsupported",
}